[
A 'Hello World!' example that uses out of bound access to data array (negative index).
This will break `bfi` but before anything bad, it detectes that and reports the error
and exits.
]

+[-->-[>>+>-----<<]<--<---]>-.>>>+.>>..+++[.>]<<<<.+++.------.<<-.>>>>+.
